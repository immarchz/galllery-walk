## Getting Started

First, setup docker then run docker compose:

```bash
docker compose up -d
```

run the development server:

```bash
npm install

npx prisma generate

npm run dev

```

run the production server:

```bash
npm run production
```
